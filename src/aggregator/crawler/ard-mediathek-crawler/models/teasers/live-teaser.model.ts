import { TeaserModel } from './teaser.model';

export class LiveTeaserModel extends TeaserModel {
  type: 'live';
  // Note: property definition incomplete (we basically only need ondemand-teaser.model.ts)
}
